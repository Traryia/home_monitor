================================================================
  home_monitor 项目 - 系统架构与需求分析
================================================================

日期: 2026-07-21  版本: v0.1

----------------------------------------------------------------
  1. 系统架构图
----------------------------------------------------------------

                    +--------------------------+
                    |     手机 / PC 浏览器      |
                    +------------+-------------+
                                 |
                    +------------v-------------+
                    |     路由器 (192.168.3.1)  |
                    +--+-----------+--------+--+
                       | WiFi      | WiFi   |
        +--------------v---+  +---v--------+ (室内)
        |  ESP32-S3 户外   |  | Pi 5 中枢  |
        |  天气采集节点     |  | 192.168.   |
        |  192.168.3.39   |  |   3.36     |
        |                  |  |            |
        | 传感器:           |  | 角色:      |
        |  BH1750 光照     |  |  MQTT Broker|
        |  可扩展:          |  |  数据存储   |
        |   SHT30/BME280   |  |  Web 服务   |
        |   雨量传感器      |  +--+--+------+
        |   土壤湿度        |     |  |
        |                  |  USB|  |网线
        +------------------+     |  |
                    +------------v+ +v----------+
                    | AM3358(BBB) | | BBB备用    |
                    | USB 通道    | | 网线通道   |
                    | 192.168.    | | 192.168.   |
                    |   7.2       | |   10.2     |
                    +------------+ +-----------+

----------------------------------------------------------------
  2. 当前状态 (v0.1-alpha)
----------------------------------------------------------------

[已完成] 硬件互联
  Pi5 <-> AM3358:  USB (192.168.7.0/30) + 网线 (192.168.10.0/30)
  Pi5 <-> ESP32:   串口 (mpremote, /dev/ttyACM1)
  ESP32 <-> WiFi:   STA 模式, IP 192.168.3.39
  SSH 认证:        Pi → BBB 免密 (ed25519 密钥)

[当前数据流] ESP32 ──(UDP/JSON每2秒)──> Pi:9999
  payload: {lux, rssi, uptime, mem_free, mem_used}

[Pi5 现有软件]
  Python 3.13     : 已安装
  Node.js 24      : 已安装
  Mosquitto 2.0.21: 已安装 (未配置运行)
  SQLite 3.46     : 已安装 (无数据库)
  nginx / web     : 未安装

[ESP32-S3 硬件能力]
  CPU:   240MHz Xtensa LX7 双核
  RAM:   512KB SRAM + 8MB PSRAM
  Flash: 16MB
  WiFi:  2.4GHz 802.11 b/g/n
  I2C x2: 可挂多个传感器 (当前用 IO43=SDA, IO44=SCL)
  ADC:   18通道 12-bit (0-3100mV, ATTN_11DB)
  GPIO:  充足空闲
  其他:  UART, SPI, I2S, 触摸, RTC

[AM3358 当前]
  OS:  Debian 13 Trixie, 5.10.168-ti-r84
  能力: 未部署任何传感器或服务, 裸系统
  GPIO: 65个 (PRU 实时协处理器可用于高速采集)

----------------------------------------------------------------
  3. 目标系统需求分析
----------------------------------------------------------------

目标: ESP32户外天气采集 + AM3358室内采集 + Pi5中枢汇总 + 手机/PC访问

[ESP32 户外需求]
  传感器建议:
    SHT30/SHT40  - I2C 温湿度 (±0.3°C, ±2%RH), 已测试通过
    BME280       - I2C 温湿度+气压 (可选,精度略低于SHT30)
    BH1750       - 光照 (已接入, 正常)
    雨量传感器    - GPIO 脉冲计数
    土壤湿度      - ADC 模拟输入
  电源: USB-C 或 5V 电池
  防水: IP65 外壳, 传感器探头外露
  通信: 需改进为 MQTT (替代当前 UDP)

[AM3358 室内需求]
  传感器建议:
    SHT30 I2C    - 室内温湿度
    CCS811/SCD40 - CO2/VOC 空气质量
  数据: MQTT → Pi5

[Pi5 中枢需求]
  MQTT Broker : Mosquitto (已安装)
  数据库     : SQLite (已安装) 或 InfluxDB
  Web 服务   : FastAPI + Vue 或 Flask + 模板
  数据保留   : 建议 30 天 (SQLite 足够)

----------------------------------------------------------------
  4. 改进建议 (按优先级)
----------------------------------------------------------------

[P0] 通信协议升级: UDP → MQTT
  问题: UDP 丢包无恢复, 无 QoS, 无时间戳, 无法双向控制
  方案: ESP32 用 umqtt.simple → Pi5 Mosquitto Broker
  收益: 可靠传输 + 双向通信 + 标准协议 + 多客户端

[P1] 数据持久化
  方案: Pi5 运行 Python MQTT subscriber → SQLite 入库
  每个设备一张表, 每小时压缩一次 (avg/max/min)

[P2] Web 仪表盘
  方案: Pi5 运行 Flask/FastAPI + 轻量前端
  功能: 实时图表, 历史查询, 设备状态

[P2] ESP32 传感器扩展
  第一步: SHT30 温湿度 (I2C, 已测试通过)
  第二步: 雨量计 (GPIO 中断)
  可选: 土壤湿度 (ADC)

[P3] AM3358 室内节点
  连接 SHT30 + CCS811 → MQTT → Pi5

[P3] 户外防护
  IP65 防水盒, 防紫外线, 防雷击浪涌
  电源: 5V 太阳能 + 锂电池 (或长 USB 供电)

----------------------------------------------------------------
  5. 技术选型理由
----------------------------------------------------------------

Mosquitto over Node-RED:
  Mosquitto 轻量 (RAM < 10MB), 稳定, 标准 MQTT 5.0
  Node-RED 更重型但可视化编程, 暂不需要

SQLite over InfluxDB:
  单节点小规模, SQLite 零运维, 数据量 < 1GB/月
  SQL 查询免费, InfluxDB 需额外维护

FastAPI/Flask over Node.js:
  Pi 已有 Python 3.13 完整栈, 避免引入第二个运行时

ESP32 I2C 引脚选择:
  推荐 IO20(SDA) + IO21(SCL)  (标准 I2C, 板载有上拉)
  当前用 IO43/44 (UART0 引脚), 改回 IO20/21 更规范

----------------------------------------------------------------
  6. 下一步行动建议
----------------------------------------------------------------

  1. [ ] 配置 Pi5 Mosquitto MQTT Broker (匿名/ACL)
  2. [ ] ESP32 固件改 MQTT, 增加 SHT30 温湿度
  3. [ ] Pi5 Python MQTT→SQLite 数据入库
  4. [ ] Pi5 Web 仪表盘 (FastAPI + ECharts)
  5. [ ] 户外防水封装 + 功耗测试
  6. [ ] AM3358 室内传感器部署


  [Dashboard v4 排查与修复] - 2026-07-21

  问题现象: http://192.168.3.36:5000/ 页面渲染正常但无任何数据。
  
  排查步骤:
  1. 服务器端验证:
     - curl http://localhost:5000/api/current -> 200, JSON包含esp32-outdoor(lux=119.2)+bbb-indoor
     - curl http://localhost:5000/api/history -> 200, JSON包含20条光照数据
     - dashboard.service -> active running
     - 文件MD5比对: Pi与本地一致
     - 结论: 服务端一切正常

  2. Windows端连通性:
     - ping 192.168.3.36 -> 通(7ms)
     - Test-NetConnection :5000 -> 通
     - Invoke-RestMethod /api/current -> 返回正确JSON
     - 结论: 网络和API均可达

  3. JS执行测试:
     - 原始方案: ECharts CDN (cdn.jsdelivr.net)在<head>同步加载 -> 大陆不可达,阻塞页面
     - 改本地ECHarts + defer: JS仍不执行
     - 最简测试(port 5002, document.title="OK"): HTML渲染但标题未变
     - 结论: 浏览器不执行任何JavaScript (Codex内置浏览器限制)

  4. 最终方案:
     - v3: 纯服务端渲染, meta refresh自动刷新, 零JS
     - v4: v3基础上增加本地ECharts图表(JS为可选增强)

  技术教训:
    - ssh heredoc经PowerShell会转义双引号(\"" -> \"), 必须用scp传文件
    - Python f-string中的JS大括号需用{{}}转义, 或用replace()替代
    - 多进程(nohup残留+systemd)同时占5000端口导致新旧版本混跑

